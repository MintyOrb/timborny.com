import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://mintyorb.github.io',
  base: '/timborny.com',
  trailingSlash: 'ignore',
});
